# pharm.jjyu.co.kr 색인 회복 런북 (2026-06-11)

## 진단 요약 (실측)

| 항목 | 수치 |
|---|---|
| 전체 spoke 글 | 3,318개 |
| 구글 색인 | 3개 / 미색인 2,250개 ("크롤링됨-색인 안 됨" 1,977) |
| 네이버 색인 | 0개 (수집제한 210, 접근불가 160) |
| 유산균 본문 평균 중복률 | 32% (1,312개 글, 가격 데이터 0건) |
| 탈모(구버전) 본문 평균 중복률 | 53%, 최대 84% (보일러플레이트 100% 글 7개+) |
| 감기·영양제 등 중복률 | 1~5% (정상) |
| v2 파이프라인 통과 글 | 5개 / 3,318개 |

**근본 원인**: ① 제품당 고유 데이터 150자로 2,500자 글 생성(유산균 기능성 문구는 법적으로 전 제품 동일) ② 유산균 1,018개 제품 가격 0건인데 "최저가" 타이틀 ③ 동일 성분 제네릭 수백 개 복붙 본문 ④ 타이틀 12개 템플릿 ⑤ 신생 도메인에 한 달 2,400페이지 일괄 발행.

## 오늘 완료된 작업

1. **타이틀 전수 교체 (3,267개 + 허브 목록 646개)** — 가격/최저가 포함 100%, 실제 가격 노출 1,582개, 의도(A/B/C/D) 배분, 제조사·함량 차별 요소. 구문 검사 통과. **아직 배포 전.**
2. **신규 스크립트 3종**
   - `scripts/assign-rewrite-plan.js` → `_workspace/rewrite-plan.json` (글별 intent·타이틀·가격)
   - `scripts/apply-titles.js` (plan → data/articles 반영)
   - `scripts/build-rewrite-queue.js` → `_workspace/rewrite-queue.json` (1,719건 우선순위), `_workspace/boilerplate-유산균.json`(302문장)·`boilerplate-탈모.json`(581문장)

## 남은 작업 (순서대로)

### 1단계: 배포 전 검증 + 타이틀 배포 (이번 주)
```
npm run quality          # 8항목 검증
npx next build           # 빌드 확인
# 배포 후 GSC·네이버에 sitemap 재제출
```

### 2단계: 본문 리라이트 — 긴급 442건부터 (보일러플레이트 50%+)
- `_workspace/rewrite-queue.json` 상위부터 `/write-spoke {slug}` 배치 실행 (일 20~40건 권장, 한 번에 몰아서 재발행 금지)
- writer 금지문장에 `boilerplate-{cat}.json` 주입 → R2-doorway 검증으로 재발 차단
- 같은 ingredientGroup은 48조합(의도6×앵커8)에 분산
- **유산균 가격 데이터 수확이 선행 과제**: data/products/유산균.ts 1,018개 price=0 → 발키리/네이버쇼핑 가격 채우기 전까지 유산균 D의도 글 작성 불가

### 3단계: 색인 운영
**구글 (GSC)**
- 리라이트 완료된 글부터 URL 검사 → 색인 요청 (일 10건 한도)
- 리디렉션 126건: `public/_redirects`의 %25 구슬러그 정리분 — 정상, 방치 가능
- 404 56건: 구 URL — sitemap에 없으면 자연 소멸
- 색인률은 "크롤링됨-색인 안 됨" 1,977 감소 추세로 모니터링 (리라이트 효과 지표)

**네이버 (서치어드바이저)**
- "접근 불가 160" 원인 확인 필수: ① 웹마스터도구 → 검증 → robots.txt 검증 실행 ② Cloudflare 대시보드 → Security → Events에서 `Yeti` UA 차단 여부 확인 (Bot Fight Mode/Browser Integrity Check가 Yeti를 챌린지하면 색인 0 지속) ③ 확인 후 수집 요청 재실행
- 네이버는 사이트 단위 신뢰가 더 보수적 → 리라이트 후 대표 글 50개 수동 수집 요청부터

### 발행 속도 원칙
한 번에 전량 재발행 금지. 카테고리 단위로 주당 100~200건씩 dateModified 갱신 → 구글이 "개선 중인 사이트"로 재평가할 시간 확보. 색인률 10%+ 회복 전에 신규 페이지 추가 금지.
